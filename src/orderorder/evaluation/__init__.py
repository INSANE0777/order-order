"""Measuring the engine instead of trusting it.

Every finding this engine makes names its own evidence, which makes each one checkable by hand — and
they have been, one at a time, in the building. What that cannot tell you is what the engine *misses*,
or how often it cries wolf on a citation that was sound all along. Those are the two numbers a reader
of the verdict board actually needs, and neither is visible from any single verdict.

So: a gold set of claim-citation pairs with known answers, and a harness that scores the engine
against it. `docs/ARCHITECTURE.md` section 11.
"""
